# Platform Client

```{note}
This is the autodoc API reference. For the guide see {doc}`../user-guide/platform`.
```

`rlmesh.platform` is experimental. It submits and reads evaluations on a managed RLMesh platform through the session that `rlmesh login` stores, with no dependencies beyond the standard library.

```{eval-rst}
.. autoclass:: rlmesh.platform.Client
   :members:
```

```{eval-rst}
.. autofunction:: rlmesh.platform.scenario_tasks
```

```{eval-rst}
.. autoclass:: rlmesh.platform.PlatformError
```
