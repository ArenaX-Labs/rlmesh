# Types

The `rlmesh.types` module defines the structural protocols accepted by `EnvServer` and shared value
aliases used by dependency-free clients.

```{eval-rst}
.. automodule:: rlmesh.types
   :members:
   :show-inheritance:
```
