# TODO(compat-matrix): The package advertises support for Python 3.10 through 3.14
# (classifiers + abi3-py311 wheel) but only 3.10 and the 3.11 dev toolchain are
# exercised today. Add one installed-wheel environment per claimed minor version
# (3.12, 3.13, 3.14) here once those interpreters are wired into the runner; this
# requires real test-harness work, not just config, so it is tracked rather than
# stubbed. The type gate now analyzes against the 3.10 floor (basedpyright
# pythonVersion), which is the minimum honest guard until the matrix lands.
[profile]
description = "Reserved for explicit local-wheel cross-version compatibility scenarios."
environments = []
